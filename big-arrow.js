const tenTimes = (num) => num*10;
const output = tenTimes(17);
console.log(output);
const getName = () => 'fsidjw sam'
const name = getName();
console.log(name);
// multiline 
const doMatch = (x, y) => {
    const sum = x+y;
    const diff = x-y;
    const result = sum+diff;
    const output = result * 5;
    return output;
}
const total = doMatch(5 ,7);
console.log(total);